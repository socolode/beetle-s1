# firmware · 固件目录

存放独角仙S1 的官方固件文件。

## 目录结构

```
firmware/
├── stable/   # 稳定版固件（推荐日常使用）
└── beta/     # 测试版固件（含新功能，可能存在不稳定）
```

## 刷机方法

1. 独角仙S1 关机状态下**按住功能键**接入电脑进入刷机模式，电脑会出现名为 `BEETLE-S1` 的 U 盘
2. 把对应版本的 `.uf2` 文件拖入 U 盘根目录
3. 设备自动重启即刷机完成

> 固件即将上传，敬请期待。
